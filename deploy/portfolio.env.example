# Copy to deploy/portfolio.env (gitignored) on the server and fill in.
# Read by the systemd unit (EnvironmentFile=).

# Your real data lives here, not the synthetic demo data/.
PORTFOLIO_DATA_DIR=data/private

# The dashboard password gate (see app.py). Use a long, unique value.
PORTFOLIO_PASSWORD=change-me
